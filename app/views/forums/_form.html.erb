<%= form_for(@forum) do |f| %>
  <% if @forum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forum.errors.count, "error") %> prohibited this forum from being saved:</h2>

      <ul>
      <% @forum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :标题 %><br />
    <%= f.text_area :title, rows: 2 %>
  </div>
  <div class="field">
    <%= f.label :内容 %><br />
    <%= f.text_area :fcontent %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
